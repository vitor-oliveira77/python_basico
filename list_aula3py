
lista_a = [1,2,3]
lista_b = [4,5,6] 
lista_c = lista_a + lista_b # concatena jogando em uma variavel
print(lista_c) 
lista_a.extend(lista_b) #estende a lista sem precisar jogar em uma variavel
